import React from "react";
import styles from "./Radio.module.scss"

const Radio = ({ lang }) => (
  <div>
    <input
      type='radio'
    >

    </input>
    <label>add {lang} joke</label>
  </div>

)

export default Radio;